<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login</title>
  <!-- Tailwind v2 CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body
  class="min-h-screen bg-gradient-to-tr from-indigo-900 via-purple-900 to-indigo-800 flex items-center justify-center px-6"
>
  <div
    class="relative bg-white bg-opacity-10 backdrop-blur-lg border border-white border-opacity-20 rounded-3xl shadow-lg max-w-md w-full p-12 text-center"
  >
    <h2 class="text-5xl font-extrabold text-white mb-10 drop-shadow-lg">Login</h2>
    <form action="/login2" method="POST" class="space-y-8">
      <input
        name="email"
        type="email"
        placeholder="Email"
        required
        class="w-full px-5 py-3 rounded-xl bg-white bg-opacity-20 placeholder-white text-white border border-white border-opacity-30 focus:outline-none focus:ring-4 focus:ring-purple-500 focus:border-purple-500 transition duration-300 shadow-sm"
      />
      <input
        name="password"
        type="password"
        placeholder="Password"
        required
        class="w-full px-5 py-3 rounded-xl bg-white bg-opacity-20 placeholder-white text-white border border-white border-opacity-30 focus:outline-none focus:ring-4 focus:ring-purple-500 focus:border-purple-500 transition duration-300 shadow-sm"
      />
      <button
        type="submit"
        class="w-full bg-gradient-to-r from-purple-600 to-indigo-700 hover:from-indigo-700 hover:to-purple-800 text-white font-extrabold py-4 rounded-xl shadow-lg hover:shadow-indigo-500 transition duration-300"
      >
        Login
      </button>
    </form>
    <div class="mt-8 text-white font-medium">
      <a href="/register" class="underline hover:text-purple-300"
        >Create/Register Account</a
      >
    </div>
  </div>
</body>
</html>
